
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: neondb_owner
      POSTGRES_PASSWORD: npg_w8WO0fnTjZKR
      POSTGRES_DB: neondb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://neondb_owner:npg_w8WO0fnTjZKR@ep-twilight-smoke-ab9enbth-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require
    depends_on:
      - db

volumes:
  pgdata:
